// import React from 'react';
// import { mount } from 'enzyme';
// import 'jest-styled-components';
// import ErrorBoundary from '.';
// import { setupMockProvider } from '../../../../testing/mocks/provider';
// import { getMockState } from '../../../../testing/mocks/mockState';

/*

  Test suite failed to run

    SyntaxError: C:\Users\Anna Branco Araujo\Documents\Personal\my-profile\src\components\core\ErrorBoundary\index.js: Support for the experimental syntax 'classProperties' isn't currently enabled (8:20):

    */

// FIX: Possible problem related to static properties

describe('< ErrorBoundary >', () => {
  //  let MockProvider;
  // let MOCK_STATE;
  //  let component;

  xit('CANNOT BE TESTED AT THIS MOMENT', () => {
    expect(true).toBe(true);
  });

  /*   beforeAll(() => {
       MockProvider = setupMockProvider({ language: 'en' }).MockProvider;
       // MOCK_STATE = getMockState({ language: 'en' });

       component = mount(
         <MockProvider>
           <ErrorBoundary />
         </MockProvider>
       );
     });

  it('should mount', () => {
    expect(component).toBeDefined();
  });

  it('matches the snapshot', () => {
    expect(component).toMatchSnapshot();
  }); */
});
